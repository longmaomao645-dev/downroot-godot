# Asset Inbox

`_inbox/` is a temporary drop zone for newly received assets.

- Move files out once their purpose is known.
- Runtime-ready assets belong in gameplay-facing folders such as `world/`, `items/`, or `production/`.
- Source sheets, previews, and editor files belong under `raw/`.
